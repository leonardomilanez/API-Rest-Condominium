package br.unesp.rc.MSCondominiumG1.dto;

/**
 * An enum to represent the different types of Area that can be created via the
 * API, allowing the Assembler to decide which entity class to instantiate.
 */
public enum AreaTypeDTO {
    COMMON,
    SPECIFIC
}
